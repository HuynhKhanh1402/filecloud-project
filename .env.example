# Global Environment Configuration
# This file contains environment variables for the entire FileCloud system

# Node Environment
NODE_ENV=production

# Port Configuration
BACKEND_PORT=3000
FRONTEND_PORT=5173

# Database Configuration
POSTGRES_HOST_PORT=5433
POSTGRES_USER=filecloud
POSTGRES_PASSWORD=filecloud123
POSTGRES_DB=filecloud
DATABASE_URL=postgresql://filecloud:filecloud123@postgres:5432/filecloud?schema=public

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=filecloud

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:5173,http://localhost:3001,http://127.0.0.1:5173
CORS_CREDENTIALS=true

# Frontend API URLs (for production build)
VITE_API_URL=http://localhost:3000
VITE_WS_URL=http://localhost:3000
